---
phase: 05-milestone-cjs-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/milestone.test.cjs]
autonomous: true
requirements: [TEST-15]

must_haves:
  truths:
    - "milestone complete with --archive-phases moves phase directories to milestones/vX.Y-phases/"
    - "archived REQUIREMENTS.md contains archive header with version, SHIPPED status, and archived date"
    - "STATE.md gets updated during milestone complete (status, activity date, description)"
    - "milestone complete handles missing ROADMAP.md without crashing"
    - "milestone complete handles empty phases directory without crashing"
  artifacts:
    - path: "tests/milestone.test.cjs"
      provides: "Extended milestone complete archiving tests"
      min_lines: 150
  key_links:
    - from: "tests/milestone.test.cjs"
      to: "get-shit-done/bin/lib/milestone.cjs"
      via: "CLI integration through runGsdTools"
      pattern: "runGsdTools.*milestone complete"
---

<objective>
Add archiving tests to the existing `milestone complete command` describe block in milestone.test.cjs.

Purpose: Validate that `milestone complete` correctly archives phase files, updates STATE.md, handles the `--archive-phases` flag, and is defensive against missing files/empty directories. This covers TEST-15.

Output: Extended milestone.test.cjs with archiving test coverage.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@tests/milestone.test.cjs
@tests/helpers.cjs
@get-shit-done/bin/lib/milestone.cjs
@get-shit-done/bin/gsd-tools.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add milestone complete archiving and STATE.md update tests</name>
  <files>tests/milestone.test.cjs</files>
  <action>
Add the following tests inside the existing `describe('milestone complete command')` block in tests/milestone.test.cjs, after the two existing tests:

1. **Test: archives phase directories with --archive-phases flag**
   - Set up: Create `.planning/ROADMAP.md`, `.planning/STATE.md`, and a phase directory `01-foundation` with a `01-01-SUMMARY.md` containing frontmatter with `one-liner` field
   - Run: `runGsdTools('milestone complete v1.0 --name MVP --archive-phases', tmpDir)`
   - Assert: Command succeeds
   - Assert: `output.archived.phases` is `true`
   - Assert: Phase directory moved — `milestones/v1.0-phases/01-foundation` exists
   - Assert: Original phase directory `phases/01-foundation` no longer exists

2. **Test: archived REQUIREMENTS.md contains archive header**
   - Set up: Create `.planning/REQUIREMENTS.md` with checkbox items, `.planning/ROADMAP.md`, `.planning/STATE.md`
   - Run: `runGsdTools('milestone complete v1.0 --name MVP', tmpDir)`
   - Read archived file at `.planning/milestones/v1.0-REQUIREMENTS.md`
   - Assert: Content includes `Requirements Archive: v1.0`
   - Assert: Content includes `SHIPPED`
   - Assert: Content includes `Archived:` with a date string
   - Assert: Original requirements content is preserved after the header

3. **Test: STATE.md gets updated during milestone complete**
   - Set up: Create `.planning/STATE.md` with `**Status:** In progress`, `**Last Activity:** 2025-01-01`, `**Last Activity Description:** Working`
   - Also create `.planning/ROADMAP.md` (needed for command to succeed)
   - Run: `runGsdTools('milestone complete v1.0 --name Test', tmpDir)`
   - Read updated `.planning/STATE.md`
   - Assert: Status line now contains `v1.0 milestone complete`
   - Assert: Last Activity Description contains `v1.0 milestone completed and archived`
   - Assert: JSON output `state_updated` is `true`

4. **Test: milestone complete handles missing ROADMAP.md gracefully**
   - Set up: Create `.planning/STATE.md` only (no ROADMAP.md, no REQUIREMENTS.md)
   - Run: `runGsdTools('milestone complete v1.0 --name NoRoadmap', tmpDir)`
   - Assert: Command succeeds (does not crash)
   - Assert: `output.archived.roadmap` is `false`
   - Assert: `output.archived.requirements` is `false`
   - Assert: `output.milestones_updated` is `true`
   - Assert: MILESTONES.md is created

5. **Test: milestone complete handles empty phases directory**
   - Set up: Create `.planning/ROADMAP.md`, `.planning/STATE.md` — phases directory exists but is empty (from createTempProject)
   - Run: `runGsdTools('milestone complete v1.0 --name EmptyPhases', tmpDir)`
   - Assert: Command succeeds
   - Assert: `output.phases` is `0`
   - Assert: `output.plans` is `0`
   - Assert: `output.tasks` is `0`

Follow existing test conventions:
- Use `createTempProject()` in beforeEach, `cleanup(tmpDir)` in afterEach (already in the describe block)
- Parse output with `JSON.parse(result.output)`
- Use `assert.ok(result.success, ...)` pattern for checking command success
- Use `fs.existsSync()` for file existence checks
- Use `fs.readFileSync()` for content verification
  </action>
  <verify>
    <automated>cd /Users/annon/projects/get-shit-done && node --test tests/milestone.test.cjs</automated>
  </verify>
  <done>5 new tests added to milestone complete describe block, all passing. Tests cover --archive-phases, archived REQUIREMENTS.md header, STATE.md updates, missing ROADMAP.md, and empty phases directory.</done>
</task>

</tasks>

<verification>
- `node --test tests/milestone.test.cjs` passes with 7+ tests (2 existing + 5 new)
- Tests cover all archiving code paths in cmdMilestoneComplete
- No production code changes — tests only
</verification>

<success_criteria>
- All milestone complete archiving tests pass
- --archive-phases flag behavior is verified (phase dirs move to milestones/vX.Y-phases/)
- Archived REQUIREMENTS.md header content validated (version, SHIPPED, date)
- STATE.md update verified (status, last activity, description)
- Defensive code paths tested (missing ROADMAP.md, empty phases)
</success_criteria>

<output>
After completion, create `.planning/phases/05-milestone-cjs-tests/05-01-SUMMARY.md`
</output>
