---
phase: 05-milestone-cjs-tests
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [tests/milestone.test.cjs]
autonomous: true
requirements: [TEST-16]

must_haves:
  truths:
    - "requirements mark-complete handles mixed prefixes (TEST-XX, REG-XX, INFRA-XX) in a single call"
    - "requirements mark-complete updates both checkboxes and traceability table rows"
    - "requirements mark-complete accepts comma-separated, space-separated, and bracket-wrapped input formats"
    - "requirements mark-complete returns not_found for invalid IDs while still updating valid ones"
    - "requirements mark-complete is idempotent — re-marking already-complete requirement does not corrupt file"
    - "requirements mark-complete returns {updated: false, reason: 'REQUIREMENTS.md not found'} when file missing"
  artifacts:
    - path: "tests/milestone.test.cjs"
      provides: "Requirements mark-complete test suite"
      min_lines: 200
  key_links:
    - from: "tests/milestone.test.cjs"
      to: "get-shit-done/bin/lib/milestone.cjs"
      via: "CLI integration through runGsdTools"
      pattern: "runGsdTools.*requirements mark-complete"
---

<objective>
Create a new `describe('requirements mark-complete command')` block in milestone.test.cjs with comprehensive tests for ID format handling, file mutations, and edge cases.

Purpose: Validate that `requirements mark-complete` correctly handles all requirement ID formats (TEST-XX, REG-XX, INFRA-XX, bare IDs), updates both checkbox and traceability table entries, accepts multiple input formats, and handles error/edge cases gracefully. This covers TEST-16.

Output: Extended milestone.test.cjs with requirements mark-complete test suite.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@tests/milestone.test.cjs
@tests/helpers.cjs
@get-shit-done/bin/lib/milestone.cjs
@get-shit-done/bin/gsd-tools.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requirements mark-complete test suite</name>
  <files>tests/milestone.test.cjs</files>
  <action>
Add a new `describe('requirements mark-complete command')` block at the end of milestone.test.cjs (after the existing `milestone complete command` describe block and after the validate consistency comment block). Use the same beforeEach/afterEach pattern with createTempProject/cleanup.

Create a helper function inside the describe block to generate a REQUIREMENTS.md with checkboxes and a traceability table:

```javascript
function writeRequirements(tmpDir, content) {
  fs.writeFileSync(path.join(tmpDir, '.planning', 'REQUIREMENTS.md'), content, 'utf-8');
}

function readRequirements(tmpDir) {
  return fs.readFileSync(path.join(tmpDir, '.planning', 'REQUIREMENTS.md'), 'utf-8');
}
```

Standard REQUIREMENTS.md fixture (use for most tests):
```markdown
# Requirements

## Test Coverage
- [ ] **TEST-01**: core.cjs has tests for loadConfig
- [ ] **TEST-02**: core.cjs has tests for resolveModelInternal
- [x] **TEST-03**: core.cjs has tests for escapeRegex (already complete)

## Bug Regressions
- [ ] **REG-01**: Test confirms loadConfig returns model_overrides

## Infrastructure
- [ ] **INFRA-01**: GitHub Actions workflow runs tests

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| TEST-01 | Phase 1 | Pending |
| TEST-02 | Phase 1 | Pending |
| TEST-03 | Phase 1 | Complete |
| REG-01 | Phase 1 | Pending |
| INFRA-01 | Phase 6 | Pending |
```

Tests to add:

1. **Test: marks single requirement complete (checkbox + table)**
   - Set up: Standard REQUIREMENTS.md fixture
   - Run: `runGsdTools('requirements mark-complete TEST-01', tmpDir)`
   - Parse output JSON
   - Assert: `output.updated` is `true`
   - Assert: `output.marked_complete` includes `TEST-01`
   - Read back REQUIREMENTS.md
   - Assert: `- [x] **TEST-01**` exists (checkbox updated)
   - Assert: `| TEST-01 | Phase 1 | Complete |` exists (table updated)
   - Assert: Other checkboxes unchanged (TEST-02 still `- [ ]`)

2. **Test: handles mixed prefixes in single call (TEST-XX, REG-XX, INFRA-XX)**
   - Set up: Standard REQUIREMENTS.md fixture
   - Run: `runGsdTools('requirements mark-complete TEST-01,REG-01,INFRA-01', tmpDir)`
   - Assert: `output.marked_complete` has length 3
   - Assert: `output.marked_complete` includes `TEST-01`, `REG-01`, `INFRA-01`
   - Read back file — all three checkboxes are now `[x]`, all three table rows are `Complete`

3. **Test: accepts space-separated IDs**
   - Set up: Standard REQUIREMENTS.md fixture
   - Run: `runGsdTools('requirements mark-complete TEST-01 TEST-02', tmpDir)`
   - Assert: `output.marked_complete` has length 2
   - Read back file — both updated

4. **Test: accepts bracket-wrapped IDs [REQ-01, REQ-02]**
   - Set up: Standard REQUIREMENTS.md fixture
   - Run: `runGsdTools('requirements mark-complete [TEST-01,TEST-02]', tmpDir)`
   - Assert: `output.marked_complete` has length 2
   - Read back file — both updated

5. **Test: returns not_found for invalid IDs while updating valid ones**
   - Set up: Standard REQUIREMENTS.md fixture
   - Run: `runGsdTools('requirements mark-complete TEST-01,FAKE-99', tmpDir)`
   - Assert: `output.updated` is `true` (at least one valid)
   - Assert: `output.marked_complete` includes `TEST-01`
   - Assert: `output.not_found` includes `FAKE-99`
   - Assert: `output.total` is `2`

6. **Test: idempotent — re-marking already-complete requirement does not corrupt file**
   - Set up: Standard REQUIREMENTS.md fixture (TEST-03 already has `[x]` and `Complete`)
   - Run: `runGsdTools('requirements mark-complete TEST-03', tmpDir)` (already complete)
   - Assert: Command succeeds
   - Read back file
   - Assert: `- [x] **TEST-03**` still present (not doubled or corrupted)
   - Assert: No `- [xx]` or other corruption in the file
   - Note: The current code uses regex that only matches `[ ]` (space), not `[x]`, so TEST-03 will end up in `not_found`. Assert accordingly: `output.not_found` includes `TEST-03`.

7. **Test: missing REQUIREMENTS.md returns expected error structure**
   - Set up: Do NOT create REQUIREMENTS.md (createTempProject does not create it)
   - Run: `runGsdTools('requirements mark-complete TEST-01', tmpDir)`
   - Assert: `output.updated` is `false`
   - Assert: `output.reason` is `'REQUIREMENTS.md not found'`

Follow existing test conventions:
- Fresh `tmpDir` from `createTempProject()` in beforeEach
- `cleanup(tmpDir)` in afterEach
- `JSON.parse(result.output)` for parsing output
- `assert.ok`, `assert.strictEqual`, `assert.deepStrictEqual` for assertions
  </action>
  <verify>
    <automated>cd /Users/annon/projects/get-shit-done && node --test tests/milestone.test.cjs</automated>
  </verify>
  <done>7 new tests in requirements mark-complete describe block, all passing. Tests cover mixed prefixes, input formats (comma/space/bracket), mixed valid/invalid IDs, idempotency, and missing REQUIREMENTS.md.</done>
</task>

</tasks>

<verification>
- `node --test tests/milestone.test.cjs` passes with 14+ tests (2 existing + 5 archiving + 7 mark-complete)
- All requirement ID format variations tested
- Both checkbox and traceability table mutations verified
- Edge cases covered (missing file, invalid IDs, idempotency)
- No production code changes — tests only
</verification>

<success_criteria>
- All requirements mark-complete tests pass
- Mixed prefix IDs (TEST-XX, REG-XX, INFRA-XX) tested in single call
- All input formats tested (comma-separated, space-separated, bracket-wrapped)
- Checkbox update (`[ ]` to `[x]`) and traceability table update (`Pending` to `Complete`) both verified
- Mixed valid/invalid IDs handled correctly
- Idempotency verified — re-marking does not corrupt
- Missing REQUIREMENTS.md error structure verified
</success_criteria>

<output>
After completion, create `.planning/phases/05-milestone-cjs-tests/05-02-SUMMARY.md`
</output>
