---
phase: 08-init-cjs-coverage
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [tests/init.test.cjs]
autonomous: true
requirements: [INIT-03, INIT-05, INIT-06]

must_haves:
  truths:
    - "cmdInitProgress enumerates phase directories and returns correct status for each (complete, in_progress, researched, pending)"
    - "cmdInitProgress identifies current_phase as first in_progress/researched phase"
    - "cmdInitProgress identifies next_phase as first pending phase"
    - "cmdInitProgress detects paused_at from STATE.md content"
    - "cmdInitQuick computes next task number from existing quick directory entries"
    - "cmdInitQuick generates correct slug from description and truncates to 40 chars"
    - "cmdInitQuick returns null slug and task_dir when no description provided"
    - "cmdInitMapCodebase lists existing .md files in codebase directory"
    - "cmdInitMapCodebase returns has_maps=false when codebase directory is empty or missing"
    - "cmdInitNewProject detects brownfield projects via package files"
    - "cmdInitNewProject detects needs_codebase_map when code exists but no .planning/codebase"
    - "cmdInitNewMilestone returns current milestone info and file existence flags"
  artifacts:
    - path: "tests/init.test.cjs"
      provides: "Test coverage for cmdInitProgress, cmdInitQuick, cmdInitMapCodebase, cmdInitNewProject, cmdInitNewMilestone"
      contains: "describe.*cmdInitProgress|describe.*cmdInitQuick|describe.*cmdInitMapCodebase|describe.*cmdInitNewProject|describe.*cmdInitNewMilestone"
  key_links:
    - from: "tests/init.test.cjs"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "CLI invocation through runGsdTools helper"
      pattern: "runGsdTools\\("
---

<objective>
Add test coverage for cmdInitProgress (phase enumeration and status detection), cmdInitQuick (quick task numbering), cmdInitMapCodebase (codebase map listing), cmdInitNewProject (brownfield detection), and cmdInitNewMilestone (milestone info).

Purpose: Addresses INIT-03 (progress), INIT-05 (quick + map-codebase), and INIT-06 (new-project + new-milestone) requirements. These are the remaining untested init functions that collectively bring init.cjs to 75%+ line coverage.

Output: Five new describe blocks appended to tests/init.test.cjs covering all major branch paths for these functions.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-init-cjs-coverage/08-CONTEXT.md
@.planning/phases/08-init-cjs-coverage/08-01-SUMMARY.md
@tests/init.test.cjs
@tests/helpers.cjs
@get-shit-done/bin/lib/init.cjs
@get-shit-done/bin/gsd-tools.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cmdInitProgress tests (INIT-03)</name>
  <files>tests/init.test.cjs</files>
  <action>
Append a cmdInitProgress describe block to tests/init.test.cjs after the Plan 01 additions. CLI command: `init progress`.

**Test cases covering all status paths (lines 599-695 of init.cjs):**

1. **No phases (empty phases dir):** Only `.planning/phases/` exists (empty). Run `init progress`. Verify: `phase_count === 0`, `phases` is empty array, `current_phase === null`, `next_phase === null`, `has_work_in_progress === false`.

2. **Multiple phases with mixed statuses:**
   - Create `.planning/phases/01-setup/` with `01-01-PLAN.md` and `01-01-SUMMARY.md` (complete: summaries >= plans > 0)
   - Create `.planning/phases/02-api/` with `02-01-PLAN.md` only (in_progress: has plans, no matching summaries)
   - Create `.planning/phases/03-ui/` with nothing or just a CONTEXT.md (pending: no plans, no research)
   Run `init progress`. Verify:
   - `phase_count === 3`
   - `completed_count === 1` (phase 01)
   - `in_progress_count === 1` (phase 02)
   - `current_phase.number === '02'` and `current_phase.status === 'in_progress'`
   - `next_phase.number === '03'` and `next_phase.status === 'pending'`
   - `has_work_in_progress === true`
   - Verify the `phases` array has entries with correct `number`, `name`, `status`, `plan_count`, `summary_count` fields.

3. **Researched status:** Create `.planning/phases/01-setup/` with only `01-RESEARCH.md` (no plans). Verify: status is `'researched'`. Also verify `current_phase` picks this up (researched counts as current).

4. **All complete:** Create `.planning/phases/01-setup/` with `01-01-PLAN.md` + `01-01-SUMMARY.md`. No other phases. Verify: `completed_count === 1`, `current_phase === null`, `next_phase === null`.

5. **Paused at detection:** Write `.planning/STATE.md` with content including `**Paused At:** Phase 2, Task 3 — implementing auth`. Run `init progress`. Verify: `paused_at` contains `'Phase 2, Task 3'`.

6. **No paused at:** Write STATE.md without any `**Paused At:**` line. Verify: `paused_at === null`.

Use createTempProject/cleanup. Write minimal file content (e.g., `# Plan` for PLAN.md, `# Summary` for SUMMARY.md).
  </action>
  <verify>
    <automated>cd /Users/annon/projects/get-shit-done && node --test tests/init.test.cjs 2>&1 | tail -5</automated>
    <manual>Check that all cmdInitProgress tests pass with correct status detection</manual>
  </verify>
  <done>cmdInitProgress describe block has 6 passing tests covering: empty phases, mixed statuses with current/next detection, researched status, all complete, paused_at detection, no paused_at</done>
</task>

<task type="auto">
  <name>Task 2: Add cmdInitQuick, cmdInitMapCodebase, cmdInitNewProject, cmdInitNewMilestone tests (INIT-05, INIT-06)</name>
  <files>tests/init.test.cjs</files>
  <action>
Append four more describe blocks to tests/init.test.cjs after the Task 1 additions.

**cmdInitQuick describe block** — CLI: `init quick [description]`. Tests:

1. **With description:** Run `init quick "Fix login bug"`. Verify: `slug === 'fix-login-bug'`, `next_num === 1`, `task_dir === '.planning/quick/1-fix-login-bug'`, `description === 'Fix login bug'`.

2. **Without description:** Run `init quick` (no args). Verify: `slug === null`, `task_dir === null`, `description === null`, `next_num === 1`.

3. **Next number increment:** Create `.planning/quick/` with directories `1-old-task/` and `3-another-task/`. Run `init quick "New task"`. Verify: `next_num === 4` (max existing + 1).

4. **Long description truncation:** Run `init quick "This is a very long description that should get truncated to forty characters maximum"`. Verify: `slug` has length <= 40.

**cmdInitMapCodebase describe block** — CLI: `init map-codebase`. Tests:

1. **No codebase dir:** Don't create `.planning/codebase/`. Run `init map-codebase`. Verify: `has_maps === false`, `existing_maps` is empty array, `codebase_dir_exists === false`.

2. **With existing maps:** Create `.planning/codebase/` with `STACK.md`, `ARCHITECTURE.md`, and `notes.txt`. Run `init map-codebase`. Verify: `has_maps === true`, `existing_maps` contains `['ARCHITECTURE.md', 'STACK.md']` (only .md files, sorted alphabetically or in readdir order), `existing_maps.length === 2`.

3. **Empty codebase dir:** Create `.planning/codebase/` (empty). Verify: `has_maps === false`, `existing_maps` is empty array, `codebase_dir_exists === true`.

**cmdInitNewProject describe block** — CLI: `init new-project`. Tests:

1. **Greenfield (no code):** Use createTempProject which has only `.planning/phases/`. Run `init new-project`. Verify: `has_existing_code === false`, `has_package_file === false`, `is_brownfield === false`, `needs_codebase_map === false`.

2. **Brownfield with package.json:** Write `package.json` to tmpDir root. Run `init new-project`. Verify: `has_package_file === true`, `is_brownfield === true`, `needs_codebase_map === true` (no `.planning/codebase/` exists).

3. **Brownfield with codebase map already present:** Write `package.json` and create `.planning/codebase/` directory. Verify: `needs_codebase_map === false` (code exists but map already present).

4. **Planning exists check:** Verify: `planning_exists === true` (createTempProject creates .planning/).

**cmdInitNewMilestone describe block** — CLI: `init new-milestone`. Tests:

1. **Basic output:** Run `init new-milestone`. Verify result has all expected fields: `current_milestone`, `current_milestone_name`, `researcher_model`, `synthesizer_model`, `roadmapper_model`, `commit_docs`, `project_path`, `roadmap_path`, `state_path`.

2. **File existence flags:** In a bare tmpDir (with .planning/ but no STATE.md, ROADMAP.md, PROJECT.md), verify: `state_exists`, `roadmap_exists`, `project_exists` are all `false`. Then create those files and verify they become `true`.

All tests use createTempProject/cleanup. For cmdInitNewProject's `find` shell-out, the real temp directory approach works since createTempProject creates clean dirs without code files — and tests that need code files can simply write a `.js` file.
  </action>
  <verify>
    <automated>cd /Users/annon/projects/get-shit-done && node --test tests/init.test.cjs 2>&1 | tail -5</automated>
    <manual>Check all cmdInitQuick, cmdInitMapCodebase, cmdInitNewProject, cmdInitNewMilestone tests pass</manual>
  </verify>
  <done>cmdInitQuick has 4 tests (with desc, without, increment, truncation). cmdInitMapCodebase has 3 tests (no dir, with maps, empty dir). cmdInitNewProject has 4 tests (greenfield, brownfield, with map, planning exists). cmdInitNewMilestone has 2 tests (basic output, file existence). All passing.</done>
</task>

</tasks>

<verification>
- `node --test tests/init.test.cjs` passes with 0 failures
- `npm test` passes (full test suite including all other test files)
- cmdInitProgress tests cover all 4 phase statuses (complete, in_progress, researched, pending)
- cmdInitQuick tests cover numbering, slug generation, truncation, and null description
- cmdInitMapCodebase tests cover missing dir, populated dir, empty dir
- cmdInitNewProject tests cover greenfield and brownfield detection paths
- cmdInitNewMilestone tests cover basic output and file existence flags
- All Plan 01 tests still pass (no regressions from appending to same file)
</verification>

<success_criteria>
- 19 new test cases across 5 describe blocks
- All tests pass via `npm test`
- No changes to production code — tests only
- Tests use CLI dispatcher (runGsdTools) per project convention
- Combined with Plan 01: all 6 requirements (INIT-01 through INIT-06) fully addressed
- init.cjs reaches 75%+ line coverage target
</success_criteria>

<output>
After completion, create `.planning/phases/08-init-cjs-coverage/08-02-SUMMARY.md`
</output>
