---
phase: 08-init-cjs-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/init.test.cjs]
autonomous: true
requirements: [INIT-01, INIT-02, INIT-04]

must_haves:
  truths:
    - "cmdInitTodos returns correct count and todo objects from populated pending directory"
    - "cmdInitTodos filters todos by area when area argument is provided"
    - "cmdInitTodos handles empty pending dir and missing pending dir gracefully"
    - "cmdInitTodos extracts title/area/created fields with correct defaults for malformed files"
    - "cmdInitMilestoneOp counts phase directories and detects completed phases via SUMMARY files"
    - "cmdInitMilestoneOp returns all_phases_complete=true when all phases have summaries"
    - "cmdInitMilestoneOp scans archive directory for archived milestones"
    - "cmdInitPhaseOp falls back to ROADMAP.md when no phase directory exists"
    - "cmdInitPhaseOp constructs synthetic phaseInfo with correct slug from roadmap phase name"
  artifacts:
    - path: "tests/init.test.cjs"
      provides: "Test coverage for cmdInitTodos, cmdInitMilestoneOp, cmdInitPhaseOp fallback"
      contains: "describe.*cmdInitTodos|describe.*cmdInitMilestoneOp|describe.*cmdInitPhaseOp"
  key_links:
    - from: "tests/init.test.cjs"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "CLI invocation through runGsdTools helper"
      pattern: "runGsdTools\\("
---

<objective>
Add test coverage for cmdInitTodos (directory reading, filtering), cmdInitMilestoneOp (phase counting, completion detection), and cmdInitPhaseOp fallback path (ROADMAP.md synthetic phaseInfo construction).

Purpose: Addresses INIT-01 (todos), INIT-02 (milestone-op), and INIT-04 (phase-op fallback) requirements. These three functions share a common pattern of reading directory contents and building structured results — grouping them keeps tests cohesive.

Output: Three new describe blocks appended to tests/init.test.cjs covering all branch paths for these functions.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-init-cjs-coverage/08-CONTEXT.md
@tests/init.test.cjs
@tests/helpers.cjs
@get-shit-done/bin/lib/init.cjs
@get-shit-done/bin/gsd-tools.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cmdInitTodos tests (INIT-01)</name>
  <files>tests/init.test.cjs</files>
  <action>
Append a cmdInitTodos describe block to tests/init.test.cjs after the existing roadmap analyze section. Use the same beforeEach/afterEach pattern with createTempProject/cleanup. CLI command: `init todos [area]`.

**Test cases (all branches in cmdInitTodos, lines 445-502 of init.cjs):**

1. **Empty pending dir:** Create `.planning/todos/pending/` directory (empty). Run `init todos`. Verify: `todo_count === 0`, `todos` is empty array, `pending_dir_exists === true`.

2. **No pending dir (outer catch):** Don't create any todos directory. Run `init todos`. Verify: `todo_count === 0`, `todos` is empty array, `pending_dir_exists === false`.

3. **Multiple todos with fields:** Create `.planning/todos/pending/` with 3 .md files:
   - `task-1.md`: `title: Fix bug\narea: backend\ncreated: 2026-02-25`
   - `task-2.md`: `title: Add feature\narea: frontend\ncreated: 2026-02-24`
   - `task-3.md`: `title: Write docs\narea: backend\ncreated: 2026-02-23`
   Verify: `todo_count === 3`, each todo has correct `title`, `area`, `created`, `file`, `path` fields.

4. **Area filter match:** Using same setup as #3, run `init todos backend`. Verify: `todo_count === 2`, `area_filter === 'backend'`, all returned todos have `area: 'backend'`.

5. **Area filter miss:** Run `init todos nonexistent`. Verify: `todo_count === 0`, `area_filter === 'nonexistent'`.

6. **Malformed file (defaults):** Create a todo file with no title/area/created fields (just `some random content`). Run `init todos`. Verify: todo has `title: 'Untitled'`, `area: 'general'`, `created: 'unknown'`.

7. **Non-md files ignored:** Create `.planning/todos/pending/` with both `task.md` and `notes.txt`. Verify only the .md file is counted.

All tests use `createTempProject()` which already creates `.planning/phases/`. Add the pending directory structure as needed in each test.
  </action>
  <verify>
    <automated>cd /Users/annon/projects/get-shit-done && node --test tests/init.test.cjs 2>&1 | tail -5</automated>
    <manual>Check that all new cmdInitTodos tests pass</manual>
  </verify>
  <done>cmdInitTodos describe block has 7 passing tests covering: empty dir, missing dir, multiple todos, area filter match, area filter miss, malformed defaults, non-md filtering</done>
</task>

<task type="auto">
  <name>Task 2: Add cmdInitMilestoneOp and cmdInitPhaseOp fallback tests (INIT-02, INIT-04)</name>
  <files>tests/init.test.cjs</files>
  <action>
Append two more describe blocks to tests/init.test.cjs after the Task 1 additions.

**cmdInitMilestoneOp describe block** — CLI: `init milestone-op`. Tests:

1. **No phases directory:** Don't create any phase directories beyond the default `.planning/phases/` (empty). Run `init milestone-op`. Verify: `phase_count === 0`, `completed_phases === 0`, `all_phases_complete === false`.

2. **Multiple phases, none complete:** Create `.planning/phases/01-setup/` and `.planning/phases/02-api/` with PLAN.md files only (no SUMMARYs). Run `init milestone-op`. Verify: `phase_count === 2`, `completed_phases === 0`.

3. **Mix of complete and incomplete:** Create `.planning/phases/01-setup/` with `01-01-PLAN.md` and `01-01-SUMMARY.md`. Create `.planning/phases/02-api/` with `02-01-PLAN.md` only. Verify: `phase_count === 2`, `completed_phases === 1`, `all_phases_complete === false`.

4. **All phases complete:** Create `.planning/phases/01-setup/` with both PLAN and SUMMARY. No other phases. Verify: `all_phases_complete === true`, `phase_count === 1`, `completed_phases === 1`.

5. **Archive directory scanning:** Create `.planning/archive/v1.0/` and `.planning/archive/v0.9/` directories. Run `init milestone-op`. Verify: `archived_milestones` contains `['v0.9', 'v1.0']` (or at least length 2), `archive_count === 2`.

6. **No archive directory:** Don't create archive dir. Verify: `archived_milestones` is empty array, `archive_count === 0`.

**cmdInitPhaseOp fallback describe block** — CLI: `init phase-op <phase>`. Tests:

1. **Normal path (directory exists):** Create `.planning/phases/03-api/` with a CONTEXT.md and PLAN.md. Write ROADMAP.md with Phase 3 entry. Run `init phase-op 3`. Verify: `phase_found === true`, `phase_dir` includes `03-api`, `has_context === true`.

2. **Fallback path (no directory, roadmap has phase):** Do NOT create a phase 5 directory. Write ROADMAP.md with `### Phase 5: Widget Builder\n**Goal:** Build widgets`. Run `init phase-op 5`. Verify: `phase_found === true`, `phase_dir === null`, `phase_slug === 'widget-builder'`, `phase_name` contains 'Widget Builder', `has_research === false`, `has_context === false`.

3. **Neither directory nor roadmap:** Don't create directory for phase 99, don't add it to ROADMAP. Run `init phase-op 99`. Verify: `phase_found === false`, `phase_dir === null`.

All tests use createTempProject/cleanup pattern. For ROADMAP tests, write a minimal ROADMAP.md with the required `### Phase N:` format.
  </action>
  <verify>
    <automated>cd /Users/annon/projects/get-shit-done && node --test tests/init.test.cjs 2>&1 | tail -5</automated>
    <manual>Check all cmdInitMilestoneOp and cmdInitPhaseOp fallback tests pass</manual>
  </verify>
  <done>cmdInitMilestoneOp has 6 tests (empty, phases no complete, mixed, all complete, archive scan, no archive). cmdInitPhaseOp fallback has 3 tests (normal, fallback with slug, neither). All passing.</done>
</task>

</tasks>

<verification>
- `node --test tests/init.test.cjs` passes with 0 failures
- New describe blocks follow existing code patterns (runGsdTools, createTempProject, cleanup)
- cmdInitTodos tests cover all branches: empty dir, missing dir, multi-file, area filter, defaults, non-md filtering
- cmdInitMilestoneOp tests cover phase counting, completion detection, archive scanning
- cmdInitPhaseOp tests cover the ROADMAP.md fallback path with synthetic phaseInfo construction
- Existing init tests (file paths, phase_req_ids) continue to pass
</verification>

<success_criteria>
- 16 new test cases across 3 describe blocks
- All tests pass via `npm test`
- No changes to production code — tests only
- Tests use CLI dispatcher (runGsdTools) per project convention
- Realistic fixture content for ROADMAP.md (full markdown structure)
</success_criteria>

<output>
After completion, create `.planning/phases/08-init-cjs-coverage/08-01-SUMMARY.md`
</output>
